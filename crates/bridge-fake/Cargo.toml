[package]
name = "limen-bridge-fake"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic M2 Bridge for lifecycle tests; never launches user content."

[dependencies]
limen-bridge-sdk = { path = "../bridge-sdk" }

[dev-dependencies]
limen-domain = { path = "../domain" }
